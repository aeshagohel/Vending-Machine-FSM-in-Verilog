# Vending-Machine-FSM-in-Verilog
This Verilog module implements a simple vending machine using a finite state machine (FSM). It accepts ₹5 and ₹10 coins, dispenses a product when the required amount is reached, and returns excess coins if overpayment occurs.
